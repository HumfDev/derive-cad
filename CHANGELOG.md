# Changelog

## Unreleased

- Milestone 1: CLI scaffold, layered config, keyring-backed secrets with file fallback,
  `derivecad init` setup wizard, sandboxed build123d subprocess execution, STEP/STL/3MF/GLB
  export, deterministic geometry inspection, and `derivecad run` wiring the fixed demo
  bracket pipeline end-to-end. CI (lint + test) on macOS/Ubuntu.
